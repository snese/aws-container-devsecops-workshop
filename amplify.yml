version: 0.1

build:
  phases:
    commands:
      - echo preBuild started on `date`
      - pip3 install mkdocs-material
      - echo build started on `date`
      - mkdocs build
  artifacts:
    baseDirectory: site
    